{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "generate": "prisma generate",
    "studio": "prisma studio",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy",
    "db:seed": "node prisma/seed.js",
    "test": "node --test src/lib/checkout.test.js",
    "lint": "find src lib prisma -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "dependencies": {
    "@portone/server-sdk": "^0.19.0",
    "@prisma/adapter-pg": "7.10.0",
    "@prisma/client": "^7.10.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "prisma": "7.10.0"
  }
}
